# review-focus

Optional **per-repo highlight focus** for the reviewer, one file per slug (`<slug>.md`). It tells the
reviewer which kinds of change to call out specifically in the highlights report (e.g. schema
migrations, API/proto definitions, the integral code wiring them up).

These files are local to your machine and git-ignored (this README is the only tracked file here).
